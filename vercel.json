{
  "framework": "nextjs",
  "buildCommand": "npm run install-ts && npm run build && node create-routes-manifest.js",
  "outputDirectory": "out",
  "installCommand": "npm install",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
} 