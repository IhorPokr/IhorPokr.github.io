{
  "framework": "nextjs",
  "buildCommand": "npm run install-ts && npm run build",
  "outputDirectory": "out",
  "installCommand": "npm install",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
} 